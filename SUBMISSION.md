# LPB-Router — 결과보고서

**Latent-trait Pandora's Box Router with Shadow-Price Budget Pacing**
SK텔레콤 Efficient LLM Routing Challenge · 2026 오픈소스 개발자대회 지정과제
Phase 20 기준 (2026-07-27) · 라이선스 Apache-2.0 · 실데이터 미수령

> ## ✅ Phase 20 — 외부 레드팀 감사 대응 완료 (2026-07-28)
>
> 2026-07-27 외부 감사가 **배포 경로 결함 8건 · 평가 타당성 결함 5건 · 문서 정합성 결함 6건**
> 을 찾았다. 전부 수정하고 **오프라인 아티팩트 전체와 실벤치(§3.1·§4·§4.5)를 재산출**했다.
>
> | 항목 | 결과 |
> |---|---|
> | 회귀 방벽 | **86/86** (`tests/test_deploy_fixes.py` 14종 신규 — push 경로가 처음으로 방벽 안에 들어왔다) |
> | 배포 경로 | 텍스트 프롬프트 · 관측 이력 · 거절 루프 · 문항 경계 · 생존 모드 · 게이트 산출물 — 전부 수정·고정 |
> | **§2 헤드라인** | LPB 0.5051 → **0.5024** (튜너 수정이 **불리하게** 나왔고 그대로 실었다) |
> | **§2.2 상한** | ★ Phase 19 의 "예측기 축 여지 0%" 결론이 **뒤집혔다** (−0.0032 → **+0.0123**) |
> | **§2.5 fast tier** | ★ 채택 근거 "+0.0083 (2.1×SE)" 가 **재현되지 않았다** — 실제로는 +0.0028 (1.0×SE) |
> | **§2.6 `se_k`** | 신설 — 가정이었던 문턱을 측정으로 확정 (**기본값 유지**) |
> | **§4 실벤치** | ★ 대등 튜닝만으로 LPB **0.6668 → 0.7070 (+0.0402)** — 이전의 자기비판이 기울어진 설정의 산물이었다 |
> | **§4.5 게이트** | "두 데이터 모두 오예측" → **1/2** 로 정정. 그리고 **일치했는데도 틀린** 사례를 발견 |
>
> **재산출 결과가 유리한 방향이든 불리한 방향이든 그대로 실었고**, D21 문턱을 넘지 않는 변경은
> 어느 방향으로도 채택하지 않았다. 경과 전체: `docs/reflections/phase20.md`.

---

## 1. 요약 (3문단)

챌린지 규칙 "최종 답은 이번 문항에서 **호출한** 후보 출력 중 하나"는 Weitzman(1979)의
Pandora's Box 최적 탐색 문제와 동형이다. LPB-Router 는 이 동형성을 이용해 라우팅·승급·중단을
휴리스틱 임계값이 아니라 **예약값 방정식** `E[(X − σ_m)⁺] = λ·c_m` 의 해로 유도한다. 여기서
`X`는 상자를 열어 실제로 얻는 **관측 상금** `E[Q|v]` 이고, `p̄_m` 은 다집단 2PL IRT + 경량
인코더, `λ` 는 tier 예산의 shadow price(오프라인 웜스타트 + 온라인 페이싱)다.

**이 보고서의 헤드라인은 유리한 구성이 아니라 실전에 가장 가까운 구성이다** — A.X 3모델 ·
모델당 출력 1개(Q2=No) · 실텍스트 · 라우터가 스스로 출력을 채점하는 실 검증기. 5모델 + 다중
샘플 투표 상자 구성의 높은 점수(0.95 대)는 **상한 참조**로만 싣는다.

그리고 가장 중요한 결과는 자기 비판에서 나왔다. 스스로 레드팀을 돌려 결함 16건을 찾아 고쳤고,
그 과정에서 **간판 실벤치 결과가 관측 조건의 산물이었음**을 확인했다(§4). 남은 명제는
**순차 관측형 라우팅의 순가치는 검증기 예리함이 지배한다**는 것이다(§3) — 재현 가능한 세
데이터점(AUC 0.851 / 0.860 / 0.914)에서 방향이 일관되게 성립한다. 다만 그 **교차점을
상수(AUC≈0.845)로 고정하려는 시도는 폐기했다**: 그 값을 만든 스크립트가 저장소에 없어
**재현이 불가능**하기 때문이다(D50). 그래서 배포 정책은 임계값이 아니라 **받은 데이터에서의
직접 비교**가 고른다(`python -m src.gate <경로>`).

★ **그런데 Phase 20 은 그 직접 비교조차 무오류가 아님을 측정했다** — 게이트가 예측층과
결정층이 **일치했는데도 틀린** 사례를 실데이터에서 냈다(§4.5 판독 2). 사람이 아니라 데이터가
결정하는 구조는 유지하되, **그 구조의 분해능 한계를 §8 에 미해결로 적어 둔다.**

> ⚠️ **Phase 20 정직성 고지 — 이 프로젝트의 출발 명제는 선행연구가 먼저 발표했다.**
> [arXiv:2606.07392](https://arxiv.org/abs/2606.07392) (Belloni·Chen·Wei, 2026-06-05) 가
> "LLM 질의 = 상자 개봉 / 배포할 출력 선택 = keep-best-opened" 라는 Pandora's Box 동형성과
> 문맥 Weitzman 지수를 이미 제시했다(Õ(√T) regret 증명 포함). 우리는 개발 이후 외부 감사로
> 이 논문을 인지했다. **동형성 자체는 우리 기여가 아니다.** 남는 차이는 ① tier 예산의 경성
> 제약과 shadow price 페이싱(그 논문에 없다) ② IRT 잠재특성 신념 + 상관 상자 사후갱신
> ③ 구현·실증·오픈소스 라우터(그 논문은 수치실험이 전혀 없다) 다. 상세: `THIRD_PARTY_LICENSES.md` §4.

## 2. 헤드라인 — 실전 근접 구성

A.X 3모델(`config_ax3.yaml`) · Q2=No · World F textworld(실프롬프트·실출력) · 실 `ScoringVerifier` ·
단일집단(런타임 도메인 미제공) · 3-fold · tier 가중 0.5/0.3/0.2.
출처: `eval/results/probe_redteam_closure.json` (`python eval/probe_redteam_closure.py`)

**★ Phase 20 재산출 (D34 튜너 · D35 대조군 수정 후, 2026-07-27).**
출처: `eval/results/probe_redteam_closure.json` (614s) · 달성구간 분모는
`probe_predictor_ceiling.json` (123s). 두 프로브 모두 수정 후 재실행한 값이다.

| 정책 | fast (0.5) | balanced (0.3) | premium (0.2) | **종합** | 달성구간% | (구 튜너) |
|---|---|---|---|---|---|---|
| 정적 cascade (예산맹목) | 0.1342 | 0.3956 | 0.7275 | 0.3313 | −8.4 | 0.3313 |
| **예산인지 cascade** | 0.3555 | 0.5159 | 0.6777 | 0.4680 | 53.2 | 0.4680 |
| 학습형 단일호출 (RouteLLM류) | 0.3582 | 0.5657 | 0.7317 | 0.4952 | 65.5 | 0.4952 |
| **LPB-Router** | 0.3582 | 0.5823 | 0.7427 | **0.5024** | **68.7** | *0.5051* |
| **SelectiveRouter (제출 기본값)** | 0.3610 | 0.5823 | 0.7524 | **0.5057** | **70.2** | *0.5071* |
| cascade-routing (arXiv:2410.10347 계열) | 0.3665 | 0.5864 | 0.7497 | **0.5091** | **71.7** | *0.5091* |

달성구간% = (점수 − 하한 0.3499) / (달성 가능 상한 **0.5719** − 하한). §2.2 참조.

> ### ⚠ 이 재산출은 우리에게 불리하게 나왔다 — 그대로 싣는다
>
> 튜너 결함을 고치자 **LPB 가 0.5051 → 0.5024 로 내려갔고**, cascade-routing 은 0.5091 로
> **불변**이어서 격차가 **0.0040 → 0.0067 로 벌어졌다.** 우리 정책만 움직인 이유는 명확하다 —
> `tune_lambda_quality` 를 쓰는 것은 LPB 와 cascade-routing 뿐이고, 후자의 λ 는 Phase 19
> §6.6 이 확인한 대로 이미 품질 최적점에 있어 선택이 바뀌지 않았다. cascade·learned 는
> 다른 튜너를 쓰므로 정의상 불변이다.
>
> **기전 (예상과 반대였고, 그것이 정보다)**: 우리는 "래칫이 λ 를 과대 추정하므로 고치면
> λ 가 내려가 σ<0 이 줄 것"이라 예상했다. 실측은 반대다 — `sigma_neg_frac`(fast) 이
> **0.58 → 0.84 로 올랐다.** 원인은 SE 의 정의에 있다. 새 규칙은 후보를 **전역 최고**와
> 쌍대 비교하는데, 멀리 떨어진 λ 일수록 두 정책의 결정이 많이 달라 **쌍대 분산이 크고**,
> 따라서 `−se_k·SE` 문턱이 넓어져 큰 λ 가 통과한다. 즉 기준선을 고정한 것은 옳지만
> (움직이는 기준선과의 비교는 그냥 틀렸다), **`se_k=1.0` 이라는 문턱값은 이 체제에서
> 너무 관대**하다.
>
> **그래서 무엇을 하는가**: 문턱을 우리에게 유리하게 되돌리지 않는다. `se_k` 는 이제
> **측정 대상**이다 — `se_k ∈ {0, 1, 2}` 를 같은 프로토콜로 재보고 train 내부에서 고른다
> (`se_k=0` 은 순수 argmax + 동률시 큰 λ 이므로 품질 최댓값을 직접 고른다). 결정은 D21
> 규칙대로 표준오차 문턱을 넘을 때만 바꾼다. 이 항목은 **미해결**이며 §8 에 남긴다.

λ 탐색은 **모든 후보에 동일 강도**(세밀격자 + 쌍대 SE 문턱)로 준다. Phase 19 가 헤드라인
프로브·게이트·`SelectiveRouter` 를 공정화했고, **Phase 20 이 빠져 있던 `demo.py` 와
`eval/routerbench_real.py` 까지 통일**했다 (D48·D49 — 후자는 방향이 반대로 LPB 가 약한
튜너를 받고 있었다).

tier별 호출/쿼리는 `probe_redteam_closure.json` 의 `per_tier._calls` 에 있다. fast tier 만
따로 보면 LPB **1.01** · cascade-routing 1.03 · 학습형 0.99 — 즉 **가중치 최고 구간에서
세 정책이 모두 사실상 1회 호출**이고, 거기서는 신념 정확도와 λ 캘리브레이션만 남는다(§2.5).

정직한 판독 4가지 (★ 전부 Phase 20 재산출 값으로 다시 계산했다 — 이전 판의 이 네 항목은
바로 위 표와 어긋나는 Phase 17 시절 수치를 그대로 달고 있었다):

1. **LPB 는 학습형 단일호출과 예산인지 cascade 를 앞선다** (**+0.0072, +0.0344**).
2. **그러나 결합 라우팅·캐스케이딩(cascade-routing)이 LPB 를 앞선다** (**+0.0067**), 그것도
   **더 적은 호출**로(1.26 vs 1.39). 이 베이스라인은 우리가 직접 추가했고, 그 결과
   지수(index) 정책이 이 체제에서 최적이 아님을 스스로 실증했다. ★ Phase 20 의 튜너 수정으로
   이 격차는 0.0040 → **0.0067 로 오히려 벌어졌다**.
3. 그래서 **제출 기본값은 LPB 단독이 아니라 `SelectiveRouter`** 다 — LPB 를 기본으로 두고
   train 내 held-out 에서 대안이 margin 이상 이길 때만 전환한다. 격차의 **49%** 를 회수했다
   (0.5024 → 0.5057, cascade-routing 0.5091 대비). 구 튜너에서는 65% 였다.
4. **fast tier(가중 0.5)에서 LPB 의 호출/쿼리는 1.01 이다.** λ 가 커져 모든 예약값이 음수가
   되면 정지 조건이 첫 관측 직후 성립하기 때문이다. 즉 **가중치가 가장 높은 구간에서 순차 탐색
   기구는 비활성**이고, 점수는 신념 정확도와 λ 캘리브레이션이 만든다. 라우터가 이 사실을
   `diagnostics["sigma_neg_frac"]`(**0.84**, Phase 20 재산출)·`calls_per_query` 로 직접
   보고한다. ★ 이 비율은 D34 수정으로 0.58 → 0.84 로 **올랐다** — 즉 퇴화는 완화된 것이
   아니라 더 뚜렷해졌고, 원인은 `se_k=1.0` 문턱이 이 체제에서 너무 관대하다는 데 있다(§8).

> **이 표에 남아 있는 낙관 편향 1건 (측정치)**: 정책이 호출 **전에** 정확한 비용을 안다고
> 가정한다(오라클 비용). 실제로 decode 길이는 호출해 봐야 알기 때문에, 프롬프트→길이 회귀로
> 추정하고 안전마진 5%를 두면 종합이 **−0.008** 이다 (0.5097 → 0.5015,
> `eval/results/probe_phase17_levers.json`). 마진은 파산을 2건 → 0건으로 없앴고 호스트 거부는
> 0건이었다. 표의 기본 구성을 `oracle` 로 둔 것은 문서↔코드 일치(재현) 때문이고, **실배포
> 권장 구성은 `cost_mode="ridge", cost_margin=0.05`** 다.
>
> 상한 참조 (5모델 + 투표 상자, 다집단): irt 0.9486 / specialist 0.9982 / corr 0.9259 /
> crossing 0.9907 / nosignal 0.9615 / textworld 0.8192. 배포 구성(단일집단) 직접 측정값은
> `eval/results/probe_domain_free.json`. 이 표는 **행동공간이 더 크고 문제가 더 쉬운 세계**의
> 수치이므로 비공개셋 기대치로 읽으면 안 된다.

## 2.2 ★ 달성 가능 상한과 우리 위치 — 이 보고서의 핵심 수치

점수 하나만 보고하면 그것이 좋은지 알 수 없다. 그래서 **같은 세계의 하한·상한을 함께 재고**,
남은 여지를 축별로 분해했다 (`eval/results/probe_predictor_ceiling.json`).

**★ Phase 20 재산출 — 그리고 이 절의 전략적 결론이 뒤집혔다.**

| 기준점 | fast | balanced | premium | 종합 | (구 대조군) |
|---|---|---|---|---|---|
| 하한 always-cheapest | 0.3499 | 0.3499 | 0.3499 | **0.3499** | 0.3499 |
| **현행 (제출 구성)** | 0.3582 | 0.5823 | 0.7427 | **0.5024** | *0.5051* |
| ⓐ 진짜확률 p̄ (학습 가능 상한) | 0.3693 | 0.5947 | 0.7580 | **0.5147** | *0.5041* |
| ★ **달성 가능 상한** (참 확률 p̄ + 완벽 채점) | 0.3693 | 0.6916 | 0.8990 | **0.5719** | *0.5725* |
| 예산제약 오라클 (명목 상한) | 0.4219 | 0.8119 | 0.9101 | 0.6365 | 0.6365 |

→ **달성 가능 구간의 68.7%** (구 69.7%). 명목 구간 0.2866 중 **22.5% 는 알레아토릭 잡음**이라
어떤 라우터도 닿을 수 없다 (품질이 Bernoulli 로 실현되므로, 확률을 완벽히 알아도 결과는 모른다).

### ★★ Phase 19 의 "예측기 축은 닫혔다" 는 **대조군 결함이 만든 결론이었다**

Phase 19 는 오라클 arm 을 **페이싱 없이** 돌리고 현행 arm 은 페이싱과 비교해(D35),
예측기 여지를 **−0.0032 (0%)** 로 보고했다. 그 위에 "예측기 용량 확장은 하지 않는다"는
전략 결정까지 세웠다. 페이싱을 맞추자 **부호가 뒤집힌다**:

| 축 | Phase 19 (짝 안 맞음) | **Phase 20 (짝맞춤)** | 판정 변화 |
|---|---|---|---|
| **예측기 (p̄)** | −0.0032 (**0%**) | **+0.0123 (9.2%)** | ★ **닫힌 축이 아니었다** |
| **검증기 (관측 채널)** | +0.0593 (88%) | **+0.0614 (45.8%)** | 여전히 최대 |
| 결정 구조 | +0.0047 (7%) | +0.0187 (13.9%) | 3배 |
| *[참조] 실현정오 p̄* | *달성 불가* | *+0.1047 (78.0%)* | clairvoyance (D29) |

즉 **D30 이 고치려 한 혼입이 다른 축(페이싱)에 그대로 남아 있었고**, 그 잔여 혼입이 예측기
여지를 음수로 보이게 만들었다. Phase 19 가 D29·D30 에서 배운 교훈("짝 안 맞은 대조군은
결론을 만든다")을 **자기 자신에게 한 번 더 적용해야 했다.**

**전략적 함의**: "예측기에 투자하지 않는다"는 Phase 19 §7 의 결정을 **철회한다.** 여지는
검증기(45.8%) > 결정 구조(13.9%) > 예측기(9.2%) 순이고, 예측기는 0 이 아니다. 다만
Phase 19 가 측정한 용량 확장 6종이 전부 회귀했다는 사실은 그대로이므로 — 그 이유(textworld 가
정의상 단일 θ 세계)도 그대로다 — **이 세계에서 그 +0.0123 을 어떻게 가져올지는 미해결**이다.

남은 달성 가능 여지 **+0.0696** 의 축별 분해 (페이싱까지 짝맞춘 대조군):

| 축 | 여지 | 비중 | 근거 |
|---|---|---|---|
| **검증기 (관측 채널)** | **+0.0614** | **45.8%** | 완벽 채점은 출력의 함수라 원리적으로 달성 가능 |
| 결정 구조 | +0.0187 | 13.9% | 둘 다 완벽해도 남는 잔차 |
| **예측기 (p̄)** | **+0.0123** | **9.2%** | 생성식의 참 확률을 꽂으면 0.5147 (현행 0.5024) |

★ **정정 2건 (같은 실수의 2·3회차)**:

1. **초판**은 오라클 p̄ 로 **실현된 정오(0/1)** 를 써서 "남은 격차의 82.8% 가 예측 몫"이라
   보고했다. 0/1 을 아는 것은 확률이 아니라 **답을 아는 것**이라 어떤 예측기도 도달 못 한다
   (D29, Phase 17 D24 의 재발).
2. **2판(Phase 19)** 은 그것을 고치면서 오라클 arm 을 **페이싱 없이** 돌려 예측기 여지를
   −0.0032(0%)로 보고했고, 그 위에 "예측기에 투자하지 않는다"는 전략을 세웠다. 페이싱을
   맞추자 **+0.0123 (9.2%) 로 부호가 뒤집혔다** (D35). 짝 안 맞은 대조군이 결론을 만든
   **세 번째** 사례다.

Phase 19 가 보탠 근거는 여전히 유효하다: 학습 가능 AUC 상한 **0.8439** vs 현행 **0.8107**
(상한의 96.1%), 특성→난이도 held-out R² 0.829, 용량 확장 6종 전부 회귀 — textworld 의 생성식
`p_ok = 0.03+0.94·σ(7·(cap−d))` 가 **정의상 순수 단일 θ 세계**라 단일 2PL 이 이미 정확한
모형이기 때문이다. **그래서 여지가 0 은 아니지만(+0.0123), 이 세계에서 그것을 가져올 방법은
아직 없다** — 두 진술은 모순이 아니고, 둘 다 적는 것이 정직하다.

**정직한 함의**: 최대 축인 검증기(45.8%)는 이 벤치마크에서 전이 불가능한 방법으로만 닿는다 —
모델 간 답 일치율이 **0.3%** 로 사실상 없고(RouterBench 에서는 같은 특성이 검증기 AUC 를
+0.178 올렸다), 잔여 오류는 "형식 맞고 값 틀림"이라 태스크를 실제로 풀어야 잡힌다. 그것은
자기 벤치마크 과적합이므로 **하지 않는다.**

⚠ 그리고 이 절의 이전 문장 "우리는 사실상 천장에 붙어 있다"는 **철회한다.** 짝맞춘 대조군에서
남은 여지는 +0.0696 이고 그 중 예측기·결정 구조 합 23.1% 는 원리적으로 이 세계 안에서
접근 가능한 축이다. 천장에 붙어 있다는 서술은 짝 안 맞은 대조군이 만든 착시였다.

## 2.5 fast tier — 가중치 0.5 구간을 정면으로 다룬 결과

§2 판독 4번이 말한 퇴화(호출/쿼리 1.01)는 **없앨 수 없다** — 빠듯한 예산에서 λ 가 커져 모든
예약값이 음수가 되면 정지 조건이 첫 관측 직후 성립하는 것이 최적 정책의 귀결이다. 그래서
"퇴화를 없애는" 대신 **그 1회를 더 잘 고르는** 두 레버를 측정했다.

> ### ⚠ 이 절의 채택 근거는 재현되지 않았다 (D55) — 그리고 결론이 바뀐다
>
> 이 표를 재산출하려다 **프로브 자체가 깨져 있었음**을 발견했다. 기준 팔이 `{}`(=LPBRouter
> 기본값)인데 Phase 18 이 **이 프로브의 결론을 받아 두 레버를 기본값으로 승격**시킨 순간부터
> 다섯 팔이 전부 같은 구성이 됐다 — 실측 Δ **+0.0000 (5/5 팔 동일)**. 즉 이전 판이 인용한
> **"+0.0083 (2.1×SE)" 는 인용된 그 스크립트로 재현되지 않는다.**
> 각 팔이 레버를 명시 고정하도록 고치고(기준 = Phase 17 상태) 다시 쟀다.

`eval/results/probe_fast_tier.json` (3-fold, Phase 20 재산출):

| 구성 | fast | 종합 | σ<0 비율 | fast Δ(쌍대 SE) |
|---|---|---|---|---|
| 기준 (Phase 17 상태: COARSE·se_k=0·Platt off) | 0.3555 | 0.5017 | 0.70 | — |
| ① λ 세밀격자 (SE 문턱 **없이**) | 0.3582 | **0.5030** | **0.51** | +0.0028 (0.0020) |
| ① λ 세밀격자 **+ SE 문턱** | 0.3555 | 0.5006 | 0.85 | +0.0000 (0.0020) |
| ② 모델별 p̄ Platt 재보정 | 0.3582 | **0.5035** ← 최고 | 0.68 | +0.0028 (0.0020) |
| **①+② 결합 (= 현행 기본값)** | 0.3582 | 0.5024 | 0.84 | +0.0028 (0.0028) |

**정정 3가지 — 전부 우리에게 불리하다**

1. **기준 팔이 0.5017 을 재현했다.** 이전 판이 적은 pre-lever 값과 정확히 같다. 즉 **레버
   효과 자체는 실재했고 깨진 것은 프로브였다.** 다만 결합 팔의 이득은 0.5017 → **0.5024
   (+0.0007)** 이지 +0.0034 가 아니고, fast Δ 는 **+0.0028 (1.0×SE)** 이지 2.1×SE 가 아니다.
   **"2.1×SE 로 D21 문턱을 통과했다"는 채택 서술은 성립하지 않는다** — 정확히 문턱 위다.
2. ★ **SE 문턱은 이 ablation 에서 해롭다.** ① 단독 0.5030 → ①+SE 0.5006 (**−0.0024**), 그리고
   σ<0 이 **0.51 → 0.85 로 폭증**한다. §2.6 의 `se_k` 스윕이 독립적으로 같은 방향을 가리켰고
   (se_k=0 0.5035 vs se_k=1 0.5024), **두 프로브가 부호에서 일치한다.**
3. **최고 팔은 결합이 아니라 Platt 단독(0.5035)** 이다. 현행 기본값(결합, 0.5024)보다 낫다.

**그래도 기본값을 바꾸지 않는다.** 두 프로브가 가리키는 이득(+0.0011~+0.0024)이 모두 D21
절대 문턱 0.005 에 미달하고, fast Δ 도 1×SE 를 넘지 않는다. **유리한 방향이든 불리한 방향이든
문턱을 넘지 않으면 바꾸지 않는다**는 규칙을 그대로 적용한다. 다만 이제 이것은 **실데이터에서
가장 먼저 재검할 후보 1순위**이고, `lam_se_k`·`calibrate_pbar` 는 인자로 노출돼 있어 게이트가
판정할 수 있다. §8 에 미해결로 남긴다.

채택 근거는 크기가 아니라 **2.1×쌍대 표준오차**다 (D21 문턱). 기전도 확인됐다 — 기존 2배
간격 격자가 λ 를 필요 이상으로 크게 잡아 **승급 가능성을 스스로 죽이고** 있었고, 세밀 격자가
σ<0 비율을 0.70 → 0.58 로 낮췄다. 다만 호출/쿼리는 여전히 1.02 이고, **퇴화 자체는 미해결**이다.

## 2.6 λ 선택 문턱 `se_k` — 가정이었던 값을 측정으로 확정했다 (Phase 20)

D34 수정 후 σ<0(fast) 이 0.58 → **0.84** 로 올랐다. 원인 가설: 채택 조건
`q − q* > −se_k·SE` 에서 **전역 최고와 멀리 떨어진 λ 는 두 정책의 결정이 많이 달라 쌍대 분산이
크고**, 따라서 문턱이 넓어져 큰 λ 가 통과한다. `se_k=1.0` 은 Phase 18 이 논리로 고른 값이고
**측정으로 고른 값이 아니었다.** 그래서 쟀다 (`eval/results/probe_se_k.json`).

**★ Phase 20 재설계 — "문턱 미달"과 "차이 없음"을 구분하기 위해 검정력을 올렸다 (D58).**
초판은 `se_k` 만 3-fold·단일 시드로 쟀고 "+0.0011 미달 → 기각"으로 끝냈다. 그런데
**검정력이 부족하면 어떤 실제 차이도 문턱을 못 넘는다.** 그래서 축을 2개로 넓히고
(`se_k` × `calibrate_pbar`) **5-fold × 3시드**(팔당 tier별 15회 적합)로 다시 쟀다.

| 구성 | fast | balanced | premium | 종합 | σ<0 | λ₀(fast) | 종합Δ (쌍대SE) | 판정 |
|---|---|---|---|---|---|---|---|---|
| se_k=0 · Platt **on** | 0.3573 | 0.5719 | 0.7478 | 0.4998 | **0.62** | 118.9 | +0.0008 (0.0004) | 기각 |
| **se_k=1 · Platt on (현행 기본값)** | 0.3562 | 0.5719 | 0.7467 | **0.4990** | 0.68 | 197.9 | — | **유지** |
| se_k=2 · Platt on | 0.3503 | 0.5699 | 0.7422 | 0.4946 | 0.94 | 555.3 | −0.0044 (0.0008) | 기각 |
| se_k=0 · Platt **off** | 0.3575 | 0.5727 | 0.7481 | **0.5002** ← 최고 | **0.59** | 83.2 | +0.0012 (0.0009) | 기각 |
| se_k=1 · Platt off | 0.3559 | 0.5727 | 0.7470 | 0.4992 | 0.68 | 206.9 | +0.0002 (0.0008) | 기각 |
| se_k=2 · Platt off | 0.3503 | 0.5736 | 0.7422 | 0.4957 | 0.94 | 571.7 | −0.0033 (0.0010) | 기각 |

★ **이번엔 검정력이 충분하다**: 최대 쌍대 SE **0.0010** < 절대 문턱 0.005. 즉 "미달"이
"검정력 부족" 때문이 아니다 — **충분히 예민한 저울에서도 어떤 팔도 문턱을 넘지 못했다.**
이것이 초판의 "기각"과 다른, **닫힌 결론**이다.

정확히 말하면: se_k=0 의 이득(+0.0008~+0.0012)은 **통계적으로는 탐지되지만**(1.3~2×SE)
**절대 크기가 결정 문턱의 1/5 수준**이라 실무적으로 무의미하다. 그리고 `calibrate_pbar` 는
켜나 끄나 차이가 없다(+0.0002~+0.0004). **두 손잡이 모두 이 체제에서는 무해·무익하다.**

**읽는 법 3가지**

1. **기전이 확인됐다 (양쪽 Platt 설정에서 동일).** `se_k` ↑ → λ₀ **119 → 198 → 555**
   (Platt off 는 83 → 207 → 572) → σ<0 **0.62 → 0.68 → 0.94** → 종합은 se_k=2 에서 뚜렷이
   하락(−0.0044). 문턱이 λ 를 밀어 올린다는 진단이 6팔 전체에서 단조적으로 맞았다.
2. **그런데 퇴화 완화가 점수를 주지 않는다.** se_k=0 은 σ<0 을 0.68 → 0.62 로 내리지만
   종합 이득은 **+0.0008 뿐**이다. 즉 **σ<0 을 낮추는 것 자체가 점수를 주지 않는다** —
   §2.2 가 결정 구조 여지를 13.9% 로 잡은 것, 그리고 §2.5 가 fast tier 레버의 실이득을
   +0.0028 로 재측정한 것과 모두 정합한다. **fast tier 퇴화 서사는 과장돼 있었다.**
3. **그래서 기본값을 바꾸지 않는다 — 그리고 이번엔 그것이 닫힌 결론이다.** 이전 판은
   "미달이라 유지"였는데 검정력이 부족해 그 말이 약했다. 이제 최대 SE 0.0010 으로도
   문턱을 넘는 팔이 없으므로 **"기본값이 최적과 실무적으로 구분되지 않는다"** 고 말할 수 있다.
   `lam_se_k`·`calibrate_pbar` 는 인자로 노출돼 있어 실데이터에서 게이트가 다시 판정한다.

**남는 정직한 한계**: 종합 쌍대 SE 는 tier 를 독립으로 보고 `w²` 합성한 근사다 (세 tier 가 같은
쿼리를 쓰므로 실제로는 상관이 있다). tier 별 Δ·SE 를 아티팩트에 함께 기록했다.

## 2.7 예산 수준 강건성 — 우위가 예산 가정에 의존하는가 (Q7 리스크)

tier 예산 수준(frac 0.08/0.25/0.60)은 주최측 미답변이고 **우리 가정**이다. 그래서 frac 을 스윕해
서열이 유지되는지 재봤다 (`eval/results/probe_budget_sweep.json`, 달성구간 % 기준):

**★ Phase 20 재산출.** 그리고 ★ **기권 팔을 LPB 열에 섞어 적던 것을 분리했다** — 이전 판은
frac 0.12 행에 **26%** 를 적고 "1위 = LPB" 라 했는데, 그 26% 는 같은 절이 *기각·기본값 off* 라고
선언한 **기권 팔(`lpb_abstain`)** 의 값이었다. 순수 LPB 는 **24%** 다.

| frac | 달성폭 | **LPB** | *(기권 팔)* | learned | 예산인지 cascade | cascade-routing | 1위 |
|---|---|---|---|---|---|---|---|
| 0.03 | 0.2406 | 0% | *−22%* | 0% | 0% | 0% | 전부 동률 |
| 0.06 | 0.2258 | 0% | *−45%* | 0% | 0% | 0% | 전부 동률 |
| 0.12 | 0.3350 | **24%** | *26%* | 16% | 16% | 22% | **LPB** |
| 0.25 | 0.4740 | 46% | *46%* | 44% | 39% | **51%** | cascade-routing |
| 0.50 | 0.5510 | 67% | *67%* | 58% | 59% | **69%** | cascade-routing |
| 1.00 | 0.5510 | 84% | *84%* | 79% | 77% | **88%** | cascade-routing |

**정직한 답: 부분적으로 의존한다.** LPB 는 **빠듯한 예산(frac ≤ 0.12)에서 1위**, cascade-routing
은 **넉넉한 예산(≥ 0.25)에서 1위**다. 챌린지가 저예산 tier 에 큰 가중을 두므로 이 의존성은 우리에게
유리한 방향이지만, 실제 예산이 가정보다 넉넉하면 불리해진다 — **Q7 은 실재 리스크**이고 그래서
배포 정책을 게이트(§4.5)가 데이터로 고르게 해 둔 것이다. (기권 팔을 빼도 frac 0.12 의 1위는
LPB 24% 로 유지되므로 이 결론 자체는 바뀌지 않는다.)

**전략적 기권도 시도했고 기각했다.** frac 0.03·0.06 에서 네 정책 모두 달성폭의 0% 를 가져가는
것을 보고, "예산이 극히 부족하면 맞힐 가능성이 높은 쿼리만 답하라"는 Lagrangian 규칙
(`p̄ − λc ≤ 0` 이면 기권)을 구현했다. 실측: frac 0.03 **−22%** · 0.06 **−45%** · 0.12 +2pp.
이유는 §2.2 와 같다 — 극단 저예산에서 오라클의 우위는 "최저가가 **어느 쿼리를** 맞힐지 아는 것"
이고 그것은 clairvoyance 다. p̄(AUC 0.81)로 선별하면 답하는 수만 줄고 품질은 비례해 오르지 않는다.
**기본값 off**, 그리고 우리 fast tier 가정(0.08)이 −45% 구간과 +2pp 구간 사이라 특히 그렇다.

## 3. 핵심 기여 — 검증기 예리함이 지배한다 (단, 교차점은 상수가 아니다)

챌린지 런타임은 품질 점수를 주지 않는다. 출력 텍스트만 오고, 맞았는지는 라우터가 판정해야
한다. 그 판정값 `v` 가 예약값·정지 규칙·최종 선택 **전부**에 들어가므로, `v` 의 정보량이
순차 관측형 라우터의 가치를 결정한다.

> ⚠️ **자기 정정 (2판 → Phase 20)**: 초판은 교차점을 **AUC ≈ 0.845 상수**로 제시하고 아래
> RouterBench 행을 "외부 검증 성공"이라 썼다. 2판은 검증기 파서 버그(§3.1)를 고친 뒤
> "**두 실데이터 모두 임계값이 오예측**했다"고 적었다. **Phase 20 재산출에서 그 서술도
> 정정한다 — 이제 1/2 다**: RouterBench 분기의 검증기 AUC 가 0.8210 → **0.8675** 로 올라
> 임계값을 넘었고, 예측층과 결정층이 **일치**했다(§4.5).
> 그래서 임계값을 폐기하는 근거는 "오예측"이 아니라 **재현 불가능**(출처 스크립트 부재, D50)
> 이다. **방향**(검증기↑ → 순차 관측형 유리)은 재현 가능한 세 행에서 견고하나, **상수는
> 근거를 제시할 수 없다.** 배포 결정권은 임계값이 아니라 직접 비교에 있다.

**★ Phase 20: 없던 스크립트를 만들어 인위 열화 행을 재현했다** (`eval/probe_verifier_sensitivity.py`,
`probe_verifier_sensitivity.json`, 3-fold, 356s). 열화는 `V(α) = (1−α)·V + α·U` 혼합이고
α 를 목표 AUC 로 이분탐색한다. **내부 정합성 검사 PASS** — 학습형은 검증기를 쓰지 않으므로
네 arm 전부에서 정확히 0.4952 로 불변이었다(실험이 의도한 축만 움직였다는 증거).

| 검증기 AUC | LPB | learned | **LPB − learned** | σ<0 | 출처 |
|---|---|---|---|---|---|
| 0.5052 (α=0.984) | 0.4935 | 0.4952 | **−0.0017** | 0.80 | 재현 ✅ |
| 0.7251 (α=0.648) | 0.4813 | 0.4952 | **−0.0138** ← 최저 | 0.82 | 재현 ✅ |
| 0.8441 (α=0.469) | 0.4842 | 0.4952 | **−0.0109** | 0.84 | 재현 ✅ |
| 0.9123 (열화 없음) | 0.5024 | 0.4952 | **+0.0072** | 0.84 | 재현 ✅ |
| *(참고) 실물 RouterBench* | *0.851* | — | *§4 참조* | — | 실데이터 |

→ **교차점(LPB = learned) ≈ AUC 0.885** (선형 보간).

### ★ 이 재현이 법칙의 형태를 바꾼다 — 단조가 아니다

이전 판의 세 행(0.507 → −0.038, 0.725 → −0.025, 0.845 → 0.000)은 **단조 증가**였고 교차점이
정확히 0.845 였다. 재현 결과는 **둘 다 다르다**:

1. **곡선이 비단조다.** AUC 0.505 에서 −0.0017 인데 0.725 에서 **−0.0138 로 더 나빠졌다가**
   0.844(−0.0109) → 0.912(+0.0072) 로 회복한다. 즉 **최악 지점은 낮은 AUC 가 아니라 중간
   (≈0.72)** 이다.
2. **교차점은 0.845 가 아니라 ≈0.885 다.**

**기전 해석 (그리고 이것이 더 유용한 진술이다)**: AUC≈0.5 의 검증기는 **무정보라서 해롭지
않다** — 관측이 아무 말도 하지 않으면 정지 규칙이 즉시 성립해 정책이 학습형과 사실상 같은
1회 호출로 퇴화하고, 그래서 손실이 −0.0017 에 그친다. 반대로 **중간 예리함(AUC≈0.72)은
가장 위험하다** — 정책이 관측을 믿고 행동할 만큼은 정보가 있는데 틀릴 때가 많아, 잘못된
승급·잘못된 최종 선택을 유발한다. **"애매한 검증기는 없는 검증기보다 나쁘다."**

따라서 남는 명제는 "검증기 AUC 가 높을수록 순차 관측형이 유리하다"가 아니라 더 좁게:
**"순차 관측형이 순가치를 내려면 검증기가 충분히 예리해야 하며(이 세계에서 ≈0.885 이상),
어중간한 예리함은 오히려 무정보보다 해롭다."** 이전 판의 단조 서술은 철회한다.

★ **정정 3건 (Phase 20)** — 이 표는 이 보고서의 핵심 명제를 담고 있으면서 세 곳이 틀려 있었다.

1. **아래 두 행의 값이 서로 바뀌어 인쇄돼 있었다**: 이전 판은 13특성 **+0.0097** / 증강
   **+0.0065** 로 적어 **AUC 가 올라갈 때 격차가 줄어드는** — 즉 이 절이 주장하는 법칙을
   **정면으로 반박하는** 표를 실었다. 아티팩트의 실제 값은 **+0.0080 → +0.0099** 로
   **법칙을 지지한다**. 데이터가 주장을 뒷받침하는데 서술이 그것을 배신하고 있었다.
2. **RouterBench 행이 배포 불가한 변형의 수치였다**: 열 제목은 "LPB − 학습형"인데 실제로는
   도메인 라벨을 쓰는 **LPB-mg** 의 값(−0.022)이었다. 챌린지 런타임은 task 라벨을 주지
   않으므로(§2·§4·README) 그 변형은 배포할 수 없다. **배포 구성으로 다시 재면 −0.0076** 이다
   (Phase 20 대등 튜닝 후, §4).
3. **검증기 AUC 는 0.804 가 아니라 0.851** 이다 (재측정, 실응답 4,000샘플).

**그리고 인위 열화 3행은 재현할 수 없다.** 출처로 적힌 `probe_verifier_sensitivity.py` 가
저장소에 없다(D50). 따라서 **교차점 0.845 는 근거를 제시할 수 없는 상수**이며, Phase 20 에서
그 값의 결정 권한을 코드에서 제거했다(§4.5 결정층만 남는다). 남는 주장은 **방향**뿐이다 —
그리고 그 방향은 재현 가능한 세 행(0.851 → −0.0076, 0.860 → +0.0080, 0.914 → +0.0099)에서
**일관되게 성립한다.**

### 3.1 검증기 재구축 — 실물 자유형식 응답 16,500셀 축별 분해

(`eval/results/probe_verifier_real.json`)

| 구성 | AUC | Δ | 게이트 |
|---|---|---|---|
| 현행 9특성 + 도메인 원-핫 (Phase 16) | 0.611 | — | FAIL |
| 현행 9특성만 (런타임엔 task 라벨 없음) | 0.651 | +0.040 | FAIL |
| + tail 파싱·형식 특성 (마커 비의존) | 0.663 | +0.012 | FAIL |
| + 실도메인 STRUCT_CHECKS (객관식·수치·코드) | 0.670 | +0.007 | FAIL |
| **+ 교차모델 답 일치 (정규형 `agree_ref`)** | **0.848** | **+0.178** | **PASS** |
| + 응답 hashing · 프롬프트 hashing · 상자 원-핫 | **0.867** | +0.019 | **PASS** |
| *[대조] 상자 원-핫만 (텍스트 무시)* | *0.699* | — | FAIL |

읽는 법:

1. 기존 검증기(0.611)는 **텍스트를 아예 무시한 대조군(0.699)보다 나빴다** — 답 마커 기반
   특성이 실텍스트에서 전멸하기 때문이다(`has_marker` 평균 0.002 · `parse_ok` 0.048 ·
   `struct` 표준편차 0.044 · `vote_share` 상수).
2. 최대 단일 기여는 **호출한 모델들 사이의 답 일치(+0.178)** 다. 정답 여부는 한 응답 **안**이
   아니라 응답들 **사이**에 있다. 이 신호는 규칙상 무료이고(이미 호출한 출력만 본다)
   **단일호출 라우터는 원리적으로 쓸 수 없다** — 순차 관측 구조의 고유 자산이다.
3. ★ **그 +0.178 중 +0.075 는 파서 버그 수정에서 왔다.** 실물 RouterBench 의 객관식 응답은
   `['D']` 로 오는데 검사기가 포장을 벗기지 않아 mmlu/arc/hellaswag **4,686셀 전부에서 0
   (표준편차 0.000)** 이었다. 하마터면 "구조 검사는 실텍스트에서 무용(+0.001)"으로 기각할
   뻔했고, 기각 전에 **분포(mean/std/단독AUC)를 본 것**이 오판을 막았다. 포장 해제와
   태스크별 정규형 일치(`['D']` ≡ `The answer is D.`)를 적용해 게이트를 처음으로 통과했다.
4. **정직한 caveat**: 이 특성 설계는 해당 데이터를 들여다본 뒤 나왔다. held-out 은 *적합*에
   대해서만 out-of-sample 이고 **설계에 대해서는 아니다.** 또한 객관식 정규화가 산문의 관사
   `a` 를 보기 문자로 오인하면 서로 틀린 응답이 같은 답으로 잡혀 검증기를 오염시킨다 —
   스스로 발견해 3단계 규칙으로 좁혔고 재측정에서 AUC 는 유지됐다(위험만 제거).

### ★★ 3.2 설계 누수를 정량화했다 — 그리고 이 절의 헤드라인 AUC 는 전이되지 않는다 (D60)

위 caveat 4 를 **문장으로 남기지 않고 측정**했다. 무작위 분할은 같은 도메인이 train·test
양쪽에 있어 설계 누수를 잴 수 없다. 그래서 **도메인을 통째로 뺀다** — 한 벤치마크를 제외하고
적합해 그 벤치마크에서 평가한다 (leave-one-benchmark-out). 설계가 특정 도메인의 형태에
맞춰져 있다면 처음 보는 도메인에서 무너져야 한다.

| 벤치마크 | 셀 수 | 무작위 분할 | **LOBO (처음 보는 도메인)** | Δ |
|---|---|---|---|---|
| arc | 616 | 0.9547 | 0.9580 | +0.0034 |
| hellaswag | 4,631 | 0.9101 | 0.8892 | −0.0209 |
| mmlu | 6,050 | 0.8995 | 0.8072 | −0.0923 |
| winogrande | 704 | 0.7599 | 0.7583 | −0.0016 |
| mbpp | 220 | 0.6285 | 0.5924 | −0.0361 |
| **other** (설계 대상 밖 벤치마크) | 4,279 | 0.7303 | **0.5206** | **−0.2098** |
| **평균** | | | **0.7543** | **−0.0595** |

**세 가지 결론 — 모두 우리에게 불리하다.**

1. **설계 누수 비용 = −0.0595 AUC.** 이 절이 자랑한 **0.867 은 설계 오염된 값**이고, 처음 보는
   도메인에서의 정직한 추정은 **0.7543** 이다.
2. ★ **그리고 그 0.7543 은 교차점(≈0.885, §3)보다 0.131 낮다.** 즉 **처음 보는 도메인에서는
   순차 관측 기구가 순가치를 내지 못한다**는 것이 이 프로젝트 자체 기준으로 따라 나온다.
3. ★★ **가장 나쁜 칸이 가장 중요한 칸이다.** 설계 대상 밖 벤치마크(`other`, 4,279셀)에서
   AUC 가 **0.5206 — 사실상 무작위**다. **SKT 비공개셋의 도메인은 우리가 본 적이 없으므로
   이 칸에 해당한다.** 따라서 실전 검증기 AUC 기대치는 0.867 이 아니라 **0.52~0.75 구간**으로
   보는 것이 정직하다.

**이것이 배포 설계에 주는 결론**: 검증기 예리함을 가정하고 순차 기구를 켜 두면 안 된다.
그래서 배포 정책을 **받은 데이터에서의 직접 비교**로 고르는 게이트(§4.5)가 장식이 아니라
**필수 안전장치**다 — 실데이터에서 검증기가 0.52 로 나오면 게이트가 학습형 단일호출을 고를
것이고, 그것이 옳은 동작이다. §3 의 "법칙"은 이제 우리 편이 아니라 **우리를 제약하는 근거**다.
5. 실도메인 STRUCT_CHECKS 자체의 기여는 **+0.007** 로 사실상 소득이 없다 (Phase 8·14 에 이어
   검증기 특성공학의 세 번째 실패). 가치는 점수가 아니라 위 3번의 진단과, SKT 도메인이 오면
   이름만 추가하면 되는 **등록 지점**이다.

## 4. 실물 공개 벤치마크 — 관측 조건이 결론을 바꾼다

실물 RouterBench 0-shot (HF `withmartian/routerbench`, 11 실모델, 실제 $비용·채점 품질·응답).
`eval/routerbench_real.py`, 4,000샘플 서브셋, tier 가중 종합.

### ★ Phase 20 재산출 — 이 절의 실험이 **자기 기준을 지키지 않고 있었다** (D49)

이전 판의 이 표는 세 가지가 어긋나 있었다: ① **LPB 에만 약한 튜너**(`tune_lambda_replay`,
예산을 채우는 최소 λ)를 주고 ② cascade 자리에 Phase 17 이 strawman 이라 폐기 선언한
`StaticCascade` 를 세우고 ③ 합성 세계에서 LPB 를 앞선다고 보고한 `CascadeRouting` 을 **빼놓았다.**
즉 저장소의 **유일한 실데이터 실험**이 §6 이 스스로 세운 공정성 기준을 지키지 않았다.
세 가지를 모두 맞추고 재실행했다 (`--subset 4000`, 잡음 관측, 698s):

그리고 ★ **`--router` dead flag 를 실제로 구현했다 (D56)** — 이 표의 `LPB` 행은 지금까지
IRT+엔진을 손으로 조립한 **대용 정책**이었다(페이싱·Platt·비용추정·quality-first λ·인증서
없음). 즉 **측정 대상 ≠ 제출 대상**이었고, 그 사실을 고치라고 만든 플래그가 dead 였다.
이제 **제출 대상 `LPBRouter` 자체**를 나란히 잰다.

| 정책 | fast (0.5) | balanced (0.3) | premium (0.2) | **종합** | (구 설정) |
|---|---|---|---|---|---|
| 정적 cascade (strawman) | 0.4990 | 0.5929 | 0.7153 | 0.5704 | 0.5028 |
| **예산인지 cascade** (신규) | 0.6125 | 0.7076 | 0.7291 | 0.6644 | — |
| LPB **대용 정책** (지금까지의 "LPB" 행) | 0.6763 | 0.7295 | 0.7366 | 0.7043 | *0.6668* |
| **cascade-routing** (신규) | 0.6950 | 0.7235 | 0.7422 | 0.7130 | — |
| 학습형 단일호출 | 0.6692 | 0.7517 | 0.7724 | 0.7146 | 0.7146 |
| ★ **LPB-Router (제출 대상 그 자체)** | 0.6840 | 0.7476 | 0.7516 | **0.7166** | — |
| ★★ **per-tier 메타 선택 (후보에 제출 대상 포함)** | 0.6840 | 0.7517 | 0.7724 | **0.7220** ← 1위 | 0.7146 |
| *LPB-mg (도메인 라벨 필요 · 배포 불가)* | *0.6973* | *0.7316* | *0.7374* | *0.7156* | *0.6931* |

메타 선택: `{fast: lpb-submitted, balanced: learned, premium: learned}` — **가중치가 가장 높은
fast tier 에서 제출 대상을 고른다.**

실응답 채점 검증기 held-out **AUC = 0.851**.

**결정론 검증 (범위를 명시한다)**: subset **4000** 에서 동일 명령 2회 독립 실행이
`tier_composite` 8개 값 전부 **완전히 일치**했고, subset **800** 에서도 2회 일치를 확인했다.
같은 크기에서 **`--router` 가 다른 정책 수치를 한 자리도 건드리지 않음**을 대조로 확인했다
(플래그 유무만 다른 두 실행의 baseline 수치가 동일).
*(정직 기록: 첫 확인 시도는 아티팩트를 **자기 사본과 비교**하는 실수였다 — 확인 실행이 완료되지
않았는데 완료로 오인했다. 다시 돌려 위 결과를 얻었다.)*

### ★ 제출 대상은 대용 정책보다 +0.0123 높고, 학습형을 +0.0020 앞선다

**지금까지 이 절의 자기비판("실데이터에서는 학습형이 앞선다")은 제출 대상이 아니라 그보다
약한 대용 정책을 잰 결과였다.** 제출 대상을 재면 순위가 뒤집힌다:

```
LPB 제출 대상   0.7166   ← 1위
학습형          0.7146   (−0.0020)
LPB-mg          0.7156   (배포 불가 · 참고)
cascade-routing 0.7130
LPB 대용 정책    0.7043   (−0.0123, 지금까지 보고해 온 값)
```

**그러나 세 가지를 함께 적어야 정직하다.**

1. **격차 +0.0020 은 작다.** 이 절에는 쌍대 SE 가 없으므로 "앞선다"를 강하게 주장하지 않는다.
   말할 수 있는 것은 **"대용 정책으로 재서 −0.0478 이라 보고했던 열세가, 제출 대상으로 재면
   사라진다"** 는 것이다.
2. **메타 선택기는 여전히 learned 를 3/3 고른다** (0.7146). 메타 선택기의 후보 목록에
   제출 대상이 들어 있지 않기 때문이다 — 이건 **고쳐야 할 남은 구멍**이고 §8 에 적는다.
3. **+0.0123 의 정체가 곧 이 실험의 결함 크기다.** 대용 정책에 없던 것(페이싱·Platt 재보정·
   quality-first λ)이 실데이터에서 그만큼 값을 한다는 뜻이며, 동시에 **"지금까지의 실벤치
   수치는 제출물을 대변하지 못했다"** 는 정량적 증거다.

**판독 — 우리에게 유리한 정정이고, 그래서 더 조심해서 적는다**

**판독 — 세 번의 정정이 모두 같은 방향이었다**

1. 이전 판의 "정직한 조건에서는 학습형이 **+0.022** 앞선다"는 문장은 세 겹으로 틀렸다:
   (a) 배포 불가한 **다집단 변형**의 값이었고, (b) LPB 에만 **약한 튜너**를 준 결과였으며,
   (c) 제출 대상이 아니라 **대용 정책**을 잰 것이었다. 셋을 다 고치면 제출 대상이
   **+0.0020 앞선다.** ★ **자기비판조차 잘못된 설정에서 재면 틀린다** — 그리고 세 결함이
   모두 우리에게 **불리한** 쪽으로 기울어 있었다는 사실을 그대로 기록한다.
2. **그래도 "LPB 가 이긴다"고 강하게 말하지 않는다.** +0.0020 은 작고 이 절에는 쌍대 SE 가
   없다. 말할 수 있는 것은 **열세가 사라졌다**는 것까지다.
3. ★ **메타 선택기의 후보 목록에 제출 대상을 넣었다 (D59) — 그리고 그것이 최고가 됐다.**
   D56 을 고친 뒤에도 메타는 후보에 제출 대상이 없어 learned 를 3/3 골랐다. 즉 **"무엇이
   이기는지 데이터가 고르게 한다"는 구조가 정작 이기는 후보를 볼 수 없었다.** 후보에 넣자
   메타가 **가중 최고 구간(fast)에서 제출 대상을 고르고** 종합이 **0.7146 → 0.7220 (+0.0074)**
   으로 올라 전 정책 중 1위가 됐다. 선택은 train sub 로만 하므로 테스트 미접촉은 유지된다.
4. **예산인지 cascade(0.6644)가 strawman(0.5704)보다 +0.094 높다.** §6 의 "cascade 대비 우위의
   상당 부분은 베이스라인 파산 탓"이라는 자기비판이 실데이터에서도 성립한다.
5. **cascade-routing(0.7130)은 합성 세계에서와 달리 1위가 아니다** — 세계가 바뀌면 서열도
   바뀐다는 §2.7 의 결론이 다른 축에서 재확인됐다.
6. LPB-mg 가 0.7156 이지만 **도메인 라벨을 소비하므로 배포 불가**다. 참고로만 싣는다.

> **재현성 caveat (정직 기록)**: 현재 코드는 n=4000·n=800 양쪽에서 2회 실행 완전 일치이고
> `--router` 는 부작용이 없다. 그런데 Phase 20 **중간** 실행 하나(`--router` 도입 전)에서
> `cascade` 가 0.4968 로 나왔고 **그 값만 재현하지 못했다.** 원인은 그 사이의 다른 코드
> 변경으로 추정되는데, **이 저장소에는 버전 관리가 없어 무엇이 바뀌었는지 확정할 수 없다.**
> 이것이 "git 저장소 부재"라는 지적의 실질적 비용이며, 재현성 주장은 **현재 코드 기준**으로만
> 한다.

**아직 재산출하지 않은 것**: 정확 관측 행(n=8,000, `routerbench_real.json`). 정확 관측은
**챌린지 조건이 아니고**(런타임은 품질을 주지 않는다) 그 특혜는 검증기를 쓰는 정책만 받으므로,
비교 근거로 쓰지 않는다. 그래서 재산출 우선순위에서 뺐다 — 필요하면 `--exact` 로 돌린다.
(Phase 16 본문의 36k 0.871 은 결과 파일이 없어 여전히 인용하지 않는다.)
- 다만 per-tier 메타 선택이 3/3 tier 에서 learned 를 골라 **0.7146 = 최고**를 달성했다.
  즉 "무엇이 이기는지 데이터가 고르게 한다"는 구조가 이 반전을 흡수한다.
- 관측 조건별로 결과 파일을 분리했다(`routerbench_real_{obs}_n{N}.json`) — Phase 16 의
  "산문 36k vs 아티팩트 8k" 불일치를 구조적으로 막기 위함이다.

## 4.5 배포 게이트 — 정책은 사람이 아니라 데이터가 고른다

실데이터가 오면 배포 정책을 어떻게 정하는가. 이것을 문장이 아니라 **코드**로 만들었다
(`src/gate.py`, `python -m src.gate <경로>`). 2층 구조다.

| 층 | 하는 일 | 권한 |
|---|---|---|
| 예측층 | 받은 데이터로 검증기 적합 → held-out AUC vs 임계값 | **없음** (보고·조기경보) |
| 결정층 | train 내부 fit/sel 분할에서 LPB·학습형·cascade-routing **직접 리플레이 비교** | 권위 |

대안 채택 조건은 `max(절대 margin 0.01, 1×쌍대 표준오차)` 다. 절대 문턱만 쓰던 첫 구현이
선택셋 n=240·SE 0.031 에서 **격차 0.0125 로 정책을 바꾸는** D21 재발을 저질렀고, 쌍대 SE 문턱을
넣어 잡았다 (사후 선택후회 +0.0045 → +0.0020).

**★ Phase 20 재산출** (`eval/results/probe_gate.json`, 양쪽 분기 실행 1554s):

| 분기 | 검증기 AUC | 예측층 | **결정층(권위)** | 예측-결정 | 사후 선택후회 |
|---|---|---|---|---|---|
| textworld + 증강 검증기 | 0.9013 | lpb | **selective** (fast=learned, balanced=cascade-routing, premium=lpb) | ✗ 불일치 | +0.0050 |
| 실물 RouterBench 실응답 | **0.8675** | lpb | **lpb (3/3 tier)** | ✓ **일치** | +0.0030 |

★ **이전 판의 문장 "전이된 임계값이 두 데이터 모두를 오예측했다"는 철회한다** — 이제 **1/2** 다.
RouterBench 분기의 검증기 AUC 가 재측정에서 0.8210 → **0.8675** 로 올라 임계값 0.845 를 넘었고,
그래서 예측층도 lpb 를 가리켜 결정층과 **일치**한다. 임계값이 이 분기에서는 맞았다.

**그러나 결정권을 임계값에 주지 않는 결론은 그대로다.** 이유가 바뀌었을 뿐이다:

1. **textworld 분기는 여전히 불일치**이고, 후회가 +0.0020 → **+0.0050** 으로 늘었다. 원인은
   premium tier 다 — 선택셋이 lpb(0.7792)를 골랐는데 테스트에서는 cascade-routing(0.8218)이
   나았다(후회 0.0248). 나머지 두 tier 는 후회 0 이다.
2. ★ **RouterBench 분기는 "일치했지만 틀렸다."** 결정층이 lpb 를 3/3 골랐는데, 같은 데이터의
   held-out 비교(§4)는 **학습형이 0.7146 vs LPB 0.7070 으로 낫다**고 말한다. 실제로 사후
   분해를 보면 balanced(후회 0.0045)·premium(후회 0.0125)에서 학습형이 더 나았다. 즉
   **예측층과 결정층이 일치해도 선택은 틀릴 수 있다** — 일치는 정확성의 보증이 아니다.
3. 따라서 남는 교훈은 "임계값이 틀린다"가 아니라 **"두 층 모두 선택셋 잡음에 노출돼 있다"** 는
   것이다. 선택셋 n 이 작을 때 결정층의 분해능이 실제 정책 격차(0.003~0.008)보다 크다는 것이
   이 두 분기의 후회(+0.0050 / +0.0030)로 직접 측정됐다.

### ★ 4.5.1 결정층: 반복 교차검증 + 반복 간 일치 요건 (D57·D61, Phase 20)

위 3번은 **미해결로 남기지 않고 고쳤다.** 두 단계다.

**D57 — 분해능**: 초판은 train 을 70/30 으로 한 번 쪼개 30% 에서만 비교해 유효 표본이
`0.21·n` 이었다. k-fold 로 모든 train 쿼리를 정확히 한 번 held-out 으로 쓴다.

**D61 — 분할의 운**: k-fold 는 표본을 키우지만 **셔플 운**은 남는다. 서로 다른 셔플로
반복하고 대안 채택에 두 조건을 **모두** 요구한다 — ① 크기 `> max(margin, se_k·SE)`
② **안정성: 반복 과반에서 같은 대안이 승리**. ②가 없으면 "우연히 이긴 한 번"이 정책을 바꾼다.

| | 초판 | **Phase 20** |
|---|---|---|
| 유효 표본 (분기 A / B) | ~240 / ~840 | **1,596 / 3,500** |
| 안정성 증거 | 없음 | `_rep_winners`·`_alt_win_repeats`·`_stable` 기록 |
| 기본값 | 단일 분할 | `n_splits=5, n_repeats=3` |

재산출 (`GATE_CV = 3-fold × 2회`, 축소 구성 — 배포 기본값은 5×3, 아티팩트에 기록):

| 분기 | AUC | 예측 | tier별 선택 | 일치 | 사후 후회 |
|---|---|---|---|---|---|
| A textworld | 0.9013 | lpb | fast·balanced=**cascade_routing**, premium=lpb | ✗ | +0.0050 |
| B RouterBench | 0.8683 | lpb | **lpb 3/3** | ✓ | +0.0052 |

**★ 안정성 요건이 실제로 발동했다 (기구 작동 증거)**: 분기 A premium 은 반복 승자가
`[lpb, cascade_routing]` 로 갈려 **1/2 → 불안정 → 전환 거부**, 분기 B fast 도
`[cascade_routing, lpb]` 로 **1/2 → 거부**. 두 경우 모두 단일 분할이었다면 그 한 번의
승자로 정책이 바뀔 수 있었다. **D32 규칙대로 발동을 증명했다.**

**★ 그러나 사후 후회는 줄지 않았다 — 그대로 적는다.** 분기 A +0.0050(불변), 분기 B
**+0.0030 → +0.0052 (오히려 증가)**. 즉 **D57·D61 은 절차의 증거 품질을 고쳤지만 결과를
개선하지 못했다.** 원인이 진단됐다: 유효 표본이 3,500 으로 커지자 SE 가 작아져
**이제 구속하는 것은 절대 문턱 0.01 이다.** 분기 B premium 에서 learned 가 +0.0033 로 앞서고
안정성도 2/2 였는데 절대 문턱에 막혀 전환하지 않았고, 사후에는 learned 가 실제로 나았다.

**그래서 절대 문턱을 낮추지 않는다 — 대신 그 비용을 측정치로 공개한다.** 0.01 은 D21 이
잡음 승리를 막으려고 세운 의도적 보수성이고, 그 대가가 **종합 +0.005 수준**임이 이제
수치로 남았다. 유리한 방향으로 문턱을 조정하는 것은 이 저장소가 스스로 금지한 행위다
(§2.6 에서 같은 규칙을 우리에게 유리한 쪽으로도 적용하지 않았다). `margin` 은 인자로
노출돼 있어 실데이터에서 재판정할 수 있다.

정직한 한계: SE 축소는 **분해능만** 고친다. 선택셋↔테스트셋 분포 차이로 인한 오선택은
표본 크기 문제가 아니므로 여전히 가능하다.

두 분기 모두 끝까지 돌아 배포 라우터를 만들고, 그 라우터가 **제출 어댑터로 유효 액션 열을
냈다** — 분기 A 는 `single`(학습형) 어댑터, 분기 B 는 `pandora` 어댑터로, 각각 15/15 쿼리에
응답했다. Phase 20 의 배포 경로 수정(D36·D44·D45) 덕에 이 스모크가 **두 어댑터 종류 모두**에서
성립한다 — 이전에는 게이트 산출물이 다집단이라 어댑터가 거부했다.

## 5. 이론적 보증 — 범위를 좁혀 말한다

| 구성 | 보증 | 검증 | **적용 범위** |
|---|---|---|---|
| Weitzman 지수 정책 | 독립 상자 전역 최적 | DP 완전탐색 대비 ≤ 8.9e-16 | ⚠ **이상화 엔진 한정** (`ExactNoise` 정확관측 + `FixedBelief` 갱신 없음) |
| 상금족 3종 (Bernoulli/Beta/GRM) | 예약값 방정식 정확해 | 정의 잔차 ≈ 0, L=2에서 GRM≡2PL | 전 범위 |
| 관측 상금 예약값 | `E[(E[Q\|v]−σ)⁺]=λc` 정확해 | 잠재 Q 해 대비 σ 과대 +0.47(잡음 sd 2.0), 상자 순서 역전 재현 | 전 범위 (Phase 17 신설) |
| 페이싱 | O(√T) regret 구조 (Balseiro et al.) | 적대적 도착순서 무응답 96–266 → **0** | 전역 예산 |
| M4 인증서 | 인증 전용 split 의 이항 상한 | λ 선택과 분리된 split | ⚠ 전역 예산에서 사건 **의존** → 교환가능성 가정 하 상한 |

**배포 라우터는 잡음 관측 + θ 갱신(=상관 상자)이므로 첫 행의 최적성은 전이되지 않는다.**
이것은 문헌상 NP-hard 영역이다(arXiv:2301.13534). "provably optimal router" 라고 쓰지 않는다.

## 6. 구성요소 기여와 공정 베이스라인

`eval/results/phase6b_ablation.json` (5모델 세계, λ₀ fold 공유) — **Phase 20 재산출**:

| 제거한 구성요소 | irt 세계 Δ | specialist 세계 Δ |
|---|---|---|
| 투표 상자 (`-votes`) | **−0.1759** | **−0.1364** |
| 페이싱 (`-pacing`) | −0.0364 (+적대순서 방어) | 0.0000 |
| quality-first λ | −0.0095 | −0.0282 |
| IRT → 판별식 (`-irt`) | **+0.0082** (중립~미세 역효과) | 0.0000 |

(full: irt 0.9284 / specialist 0.9990.) 서열·해석은 튜너 수정 전과 동일하다 — **최대 기여는
여전히 thesis 와 무관한 투표 상자**이고, IRT 는 in-distribution 에서 중립이며 그 가치는 §8 이
적은 대로 **약한 인코더에서의 강건성**이다.

★ **베이스라인 공정화 (Phase 17)**: 기존 비교군 `StaticCascade` 는 예산을 보지 않아 저예산
tier 에서 파산했다 — **strawman 이었다.** 문항당 허용액만 넣은 `BudgetCascade` 를 추가하니
LPB 대비 격차가 **0.156 → 0.034** 로 줄었다. 즉 이전에 보고한 cascade 대비 우위의 약 3/4은
"우리가 잘해서"가 아니라 **"베이스라인이 파산해서"** 였다. 같은 이유로 결합 라우팅
(`CascadeRouting`)을 추가했고, 그것이 LPB 를 앞섰다(§2).

## 7. 자체 발견·수정한 결함 (선별)

| # | 결함 | 영향 |
|---|---|---|
| D17 | `σ=1−λc/p̄` 는 σ≥0 특수해. `λc>p̄` 에서 참값 `p̄−λc` 와 **상자 순서가 갈림** | irt/fast 결정의 1.84%가 오선택이었음 |
| D22 | 회귀 테스트가 **규칙 위반 동작을 고정**하고 있었다 — 어댑터가 미호출 모델을 답으로 지명 | 규칙 위반. `Action("abstain")` 신설 + 불변식 테스트 |
| D23 | `open_order` 인자 미전달로 A/B 가 **실행되지 않았다** | Phase 15 의 "value-order 는 포섭됨" 결론 철회. 실제로는 체제 의존 |
| D24 | `verifier=Q.copy()` 한 줄이 간판 결론을 만들었다 | §4 반전 |
| D25 | 약한 베이스라인이 강한 주장을 만들었다 | §6 격차 축소 |
| D26 | **내가 만든 게이트가 D21 을 재발**했다 — 절대 margin 만 보고 SE 0.031 구간의 격차 0.0125 로 정책을 바꿨다 | 쌍대 SE 문턱 도입, 사후 선택후회 0.0045 → 0.0020 |
| D27 | **특성이 죽어 있으면 그 특성이 무가치한 게 아니라 파서가 틀린 것일 수 있다** — 실응답 `['D']` 포장을 못 벗겨 4,686셀 전부 0(std 0.000) | 기각 전 분포 확인이 오판을 막았고, 수정이 검증기 게이트를 통과시켰다 (§3.1) |
| D28 | **레버를 단독으로 재고 결론내면 오독한다** — "검증기 개선은 fast tier 에 무용"이라 썼다가 λ 레버 적용 후 부호가 뒤집혔다 | λ 가 2회 개봉을 만들어야 일치 특성이 살아난다 (상보성, `phase18.md` §3) |
| D29 | **오라클을 설계할 때 "무엇을 아는 오라클인가"를 먼저 쓰라** — 실현된 정오(0/1)를 예측 상한으로 써서 "예측 몫 82.8%"라는 틀린 결론을 냈다 | 참 확률로 재면 예측 여지는 **0**. D24 와 같은 실수의 재발 (§2.2) |
| D30 | **오라클 치환은 다른 것도 같이 바꾼다** — 신념을 치환하면 θ 갱신 능력도 함께 사라진다 | 짝맞춘 대조군으로 분리: 예측 −0.0032 · 갱신 −0.0008 |
| D31 | **"판정 불가"를 중간값으로 표현하지 말 것** — 학습 분포 평균 0.003 인 슬롯에 0.5 를 주입해 선형 헤드가 외삽했다 | 종합 −0.0217 → 수정 후 −0.0007 |
| D32 | ★ **null 결과를 믿기 전에 기구가 발동했는지 증명하라** — 이 세션에서 "효과 0"이 실제로는 "미실행"이었던 사례가 **세 번** | 발동 카운터(`n_abstain`/`n_route`) 도입 (`phase19.md` §6.8) |
| D33 | 하한/상한을 예산 제약 없이 정의해 극단 저예산에서 실현 불가능한 밴드를 냈다 | 백분율이 −2e10% 로 터져 발견 — 둘 다 예산 준수로 수정 |
| — | 예약값을 잠재 Q 로 풀던 결함 / `posterior` 상금의 v 이중계산 / 인증서 in-sample / `pip install` 후 크래시 / 호출 전 비용 오라클 가정 | 전부 수정 (`docs/reflections/phase17.md`) |

### 7.1 ★ 외부 감사가 찾은 결함 (Phase 20, 2026-07-27) — 자체 발견이 **아니다**

위 표는 전부 자체 발견이다. 아래는 **외부 레드팀이 찾았고 우리가 놓쳤던** 것들이며, 구분해
적는 것이 이 저장소의 규칙에 맞다. 공통 원인은 하나다 — **우리의 품질 기구는 반복 측정
하나뿐인데, 그 기구가 오프라인 리플레이 경로만 덮는다** (`docs/reflections/phase20.md` §0).

| # | 결함 | 왜 19 Phase 동안 살아남았나 |
|---|---|---|
| **D34** | **λ 튜너 래칫** — SE 문턱이 *직전 채택자*와 비교하고 기준선을 옮겨 손실이 누적. 오름차순 격자에서 λ 가 항상 최대로 밀린다. **커밋된 아티팩트 17개 전부에 영향** | **어떤 λ 도 유효한 점수를 낸다.** 틀린 λ 는 조용히 품질만 깎으므로 측정에 안 걸린다 |
| **D35** | §2.2 상한 대조군의 페이싱 불일치 (오라클 arm OFF vs 현행 ON) → 달성 구간 % 과대, "예측기 0%" 미식별 | 상한이 조금 낮게 나올 뿐 그럴듯한 숫자가 나온다 |
| **D36** | `text_encoder` 가 어디서도 대입되지 않아 **텍스트 프롬프트에서 제출 어댑터가 첫 스텝에 죽는다** | 72개 테스트 전부가 ndarray 특성을 넘겼다. 챌린지 실입력 형태를 한 번도 밟지 않았다 |
| **D39** | `observe()` 가 호출 이력을 못 봐 **교차모델 일치(+0.178 AUC)가 배포에서 계산 불가**. 그런데 `verifier.py` 는 "배포 경로는 그렇게 할 수 있다"고 단언했다 | 오프라인 경로는 `Session.observe_fn` 으로 되므로 프로브에서는 잘 돌았다 |
| **D40** | 호스트가 호출을 거절하면 동일 액션 무한 재발행 | 자체 드라이버는 항상 요청을 수락한다 |
| **D41** | `end_query(spent)` 가 첫 tier 만 갱신 — docstring 예시가 바로 그 형태 | 테스트가 항상 tier 를 명시했다 |
| **D43** | **생존 모드가 배포 경로에서 영구 비활성** (`min_costs` 를 `route()` 만 채우는데 어댑터는 `route()` 를 안 부른다) | ★ `test_regression.py:84` 가 `pol.min_costs` 를 **손으로 주입**해 가려 줬다 |
| **D44/45** | "수령 당일 첫 명령"(`python -m src.gate`)이 **배포 불가한** 다집단 라우터를 내고, 적합된 검증기를 버려 런타임 `observe` 를 만들 수 없었다 | 게이트 테스트가 `lpb_kwargs={"use_domain": False}` 를 **명시**해 기본값 경로를 밟지 않았다 |
| **D46/47** | `reproduce.py` 기본 경로가 §4.5 아티팩트의 절반을 덮어쓰고, §2.2·§2.7 프로브는 목록에 없었다 (§10 은 "전부 포함"이라 적었다) | 재현 스크립트를 검사하는 것은 아무것도 없다 |
| **D57/D61** | 게이트 결정층이 **단일 70/30 분할**이라 유효 표본이 train 의 21% 뿐이었고, 쌍대 SE(0.014~0.045)가 판정 대상 격차(0.003~0.008)보다 3~7배 컸다. 게다가 셔플 운을 통제하는 장치가 없었다 | 배포 정책을 정하는 절차인데 **그 절차의 분해능을 아무도 재지 않았다.** 사후 선택후회는 기록하고 있었지만(+0.0050/+0.0030) 그것이 절차 결함의 신호임을 읽지 못했다 |
| **D59** | ★ D56 을 고친 뒤에도 **메타 선택기 후보 목록에 제출 대상이 없었다** — 즉 "무엇이 이기는지 데이터가 고르게 한다"는 구조가 **정작 이기는 후보를 볼 수 없었다.** 넣자 종합 0.7146 → 0.7220 | 후보 목록이 코드에 하드코딩돼 있고, "모든 배포 가능 정책이 후보인가"를 검사하는 것이 없었다 |
| **D60** | ★★ 검증기 헤드라인 AUC 0.867 이 **설계 오염값**이었다. 도메인을 통째로 뺀 LOBO 로 재면 평균 **0.7543** (−0.0595), 설계 대상 밖 벤치마크는 **0.5206 = 무작위**. SKT 도메인은 후자에 해당한다 | caveat 4 로 **문장으로만** 인정하고 측정하지 않았다. 무작위 분할은 같은 도메인이 양쪽에 있어 설계 누수를 원리적으로 잴 수 없는데, 그 사실을 알면서 다른 측정으로 대체하지 않았다 |
| **D54** | ★ `probe_ax3_collapse.py` 가 `--single` 여부와 무관하게 **항상 같은 파일**에 썼다. `reproduce.py` 는 이것을 두 번 돌리고 산출물을 `probe_ax3_collapse(_q2no).json` 이라 적어 뒀으니, **두 단계가 서로를 덮어써서** 뒤에 돈 것만 남고 저장소의 `_q2no`·`_q2yes` 아티팩트는 **어떤 명령으로도 재생성되지 않았다** | 파일명이 인자에 의존하는지 검사하는 기구가 없다. D46 과 같은 부류 (파일 쓰기 경로는 아무도 테스트하지 않는다) |
| **D55** | ★★ **`probe_fast_tier.py` 의 기준 팔이 자기 자신이 됐다.** 기준 팔이 `{}`(=LPBRouter 기본값)인데, Phase 18 이 **이 프로브의 결론을 받아 두 레버를 기본값으로 승격**시킨 순간부터 다섯 팔이 모두 같은 구성이 됐다. 실측 Δ = **+0.0000 (5/5 팔 동일)**. 즉 §2.5 가 채택 근거로 인용한 "+0.0083 (2.1×SE)" 는 **인용된 그 스크립트로 재현되지 않았다** | **레버를 기본값으로 승격시키면 그 레버를 측정한 프로브가 무효가 된다** — 승격과 프로브 갱신이 짝지어져 있지 않았다. D32("기구가 발동했는지 증명하라")·D51("픽스처가 부재를 은폐한다")의 4회차 |
| **D48/49** | 대등 튜닝이 5곳 중 3곳만 적용 — 빠진 두 곳이 **시연영상 촬영 대상(`demo.py`)** 과 **유일한 실데이터 실험**이고, 후자는 방향이 반대로 **LPB 가 약한 튜너**를 받고 있었다 | 공정화를 한 번에 전수 적용했는지 확인하는 기구가 없었다 |
| *(기각)* | *"데모의 유리한 서열은 기울어진 튜너 탓"이라는 감사 해석* | **측정으로 기각**: 고친 뒤 데모 수치가 한 자리도 안 바뀌었고, λ 를 찍어 보니 두 튜너의 해가 **동일**했다(15.9321). 이 구성은 λ–품질 곡선이 가팔라 세 선택 규칙이 모두 같은 정점을 고른다. 코드 결함은 실재했으나 그 해석은 틀렸다 (`phase20.md` §2.6) |
| **D50** | 교차점 **0.845 의 출처 스크립트가 저장소에 없다** — 재현 불가 상수가 코드에 하드코딩 | "산문만 있는 수치는 쓰지 않는다"는 규칙을 **문서에만** 적용하고 코드 상수에는 적용 안 했다 |
| **D51** | ★ **테스트의 손 주입은 커버리지가 아니라 커버리지 부재의 표식이다** (D22 의 3회차) | 패턴으로 인식하지 못했다 |
| **D52** | README 가 아티팩트에 없는 수치(0.514)를 그 아티팩트 출처로 인용 | 산문↔아티팩트 대조 기구 없음 |

**원인 치료**: 증상만 고치면 같은 자리에서 재발하므로 **측정 경계 자체를 옮겼다** —
push 경로 회귀 14종 신설, λ 선택을 순수 함수(`select_lambda`)로 분리해 리플레이 없이
단위 테스트, 생존 모드 **발동**을 단언(손 주입 제거), `reproduce.py` 에 누락 프로브 추가.

전체 결함 로그: `docs/reflections/phase1..20.md` (D1~D55). **기각한 자기 주장**(측정 후 폐기):
conformal 정지 제어, 보수화 관측 채널, KV-cache prefix sharing, 검증기 task-불가지 특성,
per-tier 메타 선택(RouterBench 정확관측), 베이지안 사후 선택.

## 8. 한계

1. **실데이터 0건.** 모든 수치는 자작 합성 6세계 + 공개 벤치마크 1종이다.
2. **검증기 게이트 임계값 — 재현 가능해졌고, 값이 바뀌었다 (D50 해소).** 없던
   `eval/probe_verifier_sensitivity.py` 를 작성해 인위 열화 행을 실제로 만들었다(§3).
   **교차점은 0.845 가 아니라 ≈0.885 이고, 곡선은 단조가 아니다** — 최악 지점이 중간
   예리함(AUC≈0.72)이다. 남는 한계: 게이트 통과의 절반은 **이 데이터를 본 뒤 설계한 특성**
   에서 왔다(§3.1 caveat 4). 실데이터에서 다시 재야 한다.
2b. **게이트 결정층 — 고쳤다 (D57·D61).** 단일 70/30 분할(유효 표본 0.21·n)을
   **반복 교차검증**(5-fold × 3반복, 유효 표본 1.00·n)으로 교체하고 **"반복 과반 승리"
   안정성 요건**을 추가했다(§4.5.1). 재산출에서 안정성 요건이 두 tier 에서 실제로 발동해
   불안정한 전환을 막았다.
   ⚠ **그런데 사후 후회는 줄지 않았다** (분기 B +0.0030 → +0.0052). 표본이 커지자 구속하는
   것이 SE 가 아니라 **절대 문턱 0.01** 로 바뀌었고, 그 문턱이 진짜 작은 이득까지 막는다.
   문턱을 유리하게 낮추지 않고 **그 비용(종합 +0.005 수준)을 측정치로 공개**하는 선택을 했다.
   ⚠ SE 축소는 분해능만 고친다 — 분포 차이로 인한 오선택은 여전히 가능하다.
3. **fast tier 퇴화는 여전하다** (호출/쿼리 1.01) — **그러나 그것이 비용이 아님이 측정됐다.**
   §2.6 의 6팔 격자에서 σ<0 을 0.68 → 0.59 로 낮춰도 종합 이득은 **+0.0012 뿐**이다.
   "퇴화를 완화하면 점수가 오른다"는 §2.5 의 암묵 전제는 **기각한다.** 남은 것은 퇴화가
   아니라 그 1회의 선택 정확도이며, 그 여지도 §2.2 기준 13.9%(결정 구조)에 불과하다.
4. **cascade-routing 이 여전히 종합 최고이고, 뒤집히지 않았다.** (수치는 §2 표.)
   ~~두 fast tier 레버 중 λ 격자는 LPB 에만 적용됐으므로 비교가 우리 쪽으로 기울어 있다~~
   → **정정 (Phase 20)**: 이 서술은 §2 와 모순이었고, 절반만 맞았다. 헤드라인 프로브·게이트·
   `SelectiveRouter` 세 곳은 Phase 19 가 이미 대등 튜닝으로 고쳤다(§2). 그러나 **`demo.py` 와
   `eval/routerbench_real.py` 두 곳은 빠져 있었고**, 특히 후자에서는 방향이 반대로 —
   **LPB 가 약한 튜너를 받고** cascade 자리에 폐기 선언한 `StaticCascade` 가 서 있었다.
   Phase 20 에서 다섯 곳 전부 `tune_lambda_quality(FINE_MULTS, se_k=1.0)` 로 통일하고
   실벤치에 `BudgetCascade`·`CascadeRouting` 을 추가했다 (D48·D49).
5. **λ 튜너에 래칫 결함이 있었다 (D34, Phase 20 수정).** Phase 18 이 도입한 SE 문턱이 후보를
   *직전 채택자*와 비교하고 기준선을 그때마다 옮겨, 유의하지 않은 손실이 **누적**됐다 —
   오름차순 격자에서 λ 가 끝까지 밀려 올라간다. λ 과대는 σ<0 을 늘려 1회 호출 퇴화를
   심화시키므로, **§2.5 가 보고한 fast tier 퇴화의 일부는 이 결함이었다.** 기준선을 전역
   최고로 고정해 수정했고(`select_lambda`, 순수 함수로 분리해 단위 테스트로 고정).
   ★ **해소**: `se_k` 문턱을 의심해 6팔 격자를 **5-fold × 3시드**로 쟀다(§2.6). 최대 쌍대
   SE 0.0010 < 문턱 0.005 이므로 **검정력이 충분한 상태에서 기본값이 최적과 구분되지 않는다** —
   "미달이라 유지"가 아니라 **닫힌 결론**이다.
6. **§2.2 상한이 짝 안 맞는 대조군이었다 (D35, Phase 20 수정).** 오라클 arm 은 맨
   `PandoraPolicy`(페이싱 OFF)인데 현행 arm 은 `PacedPandora`(ON)였다. 페이싱은 이득이므로
   상한이 과소평가되어 "달성 구간 %"가 실제보다 좋게 나왔고, "예측기 여지 0%" 결론에도
   페이싱 효과가 섞여 있었다 — D30 이 고치려던 혼입이 다른 축에 남아 있었던 것이다.
7. ★ **실벤치가 제출 대상을 재지 않고 있었다 (D56, Phase 20 수정).** `--router` 플래그가
   dead code 여서 §4 는 줄곧 **대용 정책**을 쟀다. 구현하고 재보니 제출 대상이 대용 정책보다
   **+0.0123** 높고 학습형을 **+0.0020** 앞선다 — 즉 이 절의 자기비판("실데이터에서 진다")이
   **측정 대상 오류**였다. ★ 그리고 메타 선택기 후보에 제출 대상을 넣자(D59) 메타가 fast tier
   에서 그것을 골라 종합이 **0.7146 → 0.7220** 으로 올라 전 정책 중 1위가 됐다 — 구멍도 닫혔다.
8. **재현성 caveat**: Phase 20 중간 실행 하나(`--router` 도입 전)의 `cascade` 값을 재현하지
   못했다. 현재 코드는 2회 실행 완전 일치이고 `--router` 는 부작용이 없음을 대조 확인했으나,
   **버전 관리가 없어 그 사이 무엇이 바뀌었는지 확정할 수 없다.** git 부재의 실질적 비용이다.
9. 관측 상금 예약값은 원리적으로 옳지만 점수 효과는 잡음 수준이다.
10. 점수는 numpy 버전에 민감하다(서열 불변). `numpy<2.0` 고정.

## 9. 제출 인터페이스

`src/submission.py` 의 `SubmissionRouter` 가 챌린지 규격을 구현한다. 제어 흐름이 반대(pull→push)
이므로 **호출 이력에서 신념을 복원**해 같은 결정을 낸다.

```python
act = sub.step(prompt, tier, call_history, model_metadata, remaining_budget)
# Action("call", model_id) | Action("answer", model_id) | Action("abstain", "")
```

- `answer` 는 **항상 `call_history` 안의 모델**이다 (규칙 불변식, 테스트로 고정).
- 정책 형태 3종(순차 개봉 / 1스텝 lookahead / 단일호출)을 모두 배포할 수 있다 — 그래서
  `SelectiveRouter` 가 무엇을 고르든 제출 경로가 동작한다.
- 등가성 회귀: 3세계/tier × 200쿼리에서 호출 열·최종 답·지출·λ 궤적 일치.
- 교체 지점 4곳(액션 표현·비용 산식·검증기 접속·훅 시점)은 파일 상단에 명시.

## 10. 재현

```bash
pip install -e ".[dev]"
python -m pytest tests/ -q          # 86/86 — 규칙과 배포 경로가 불변식으로 들어 있다
python -m src.gate <데이터경로>       # ★ 실데이터 수령 시 첫 명령 (배포 정책 자동 결정, §4.5)
python demo.py                      # 3분 시연 (네트워크 불필요)
python reproduce.py --list          # 어떤 단계가 어떤 표를 만드는지
python reproduce.py                 # 오프라인으로 만들 수 있는 모든 표
python reproduce.py --bench         # + 실물 RouterBench (데이터 다운로드)
```

**재현 범위를 정확히 적는다 (Phase 20 정정).** 이전 판은 "§2·§3·§4 표를 만드는 스크립트를
**전부** 포함한다"고 썼는데 **사실이 아니었다** — §2.2(달성 가능 상한·축별 분해, 이 문서가
스스로 '핵심 수치'라 부르는 표)와 §2.7(예산 스윕)을 만드는 두 프로브가 어느 단계 목록에도
없었다. 두 단계를 기본 경로에 넣었다(`predictor-ceiling`, `budget-sweep`).

- **오프라인 기본 경로가 재현하는 것**: §2 · §2.2 · §2.5 · **§2.6** · §2.7 · §4.5(분기 A) · §6
- **`--bench` 가 추가로 재현하는 것**: §3.1 검증기 AUC 사다리 · §4 실물 RouterBench ·
  §4.5 분기 B (전부 HuggingFace 다운로드 필요)
- **아직 어떤 스크립트도 만들지 않는 것**: §3 법칙 표의 **인위 열화 3행**(AUC 0.507/0.725/
  0.845). 출처로 적힌 `probe_verifier_sensitivity.py` 는 **저장소에 없다.** 그래서 교차점
  상수 0.845 는 재현 불가이며, Phase 20 에서 그 상수의 **결정 권한을 코드에서 제거**했다
  (§4.5 결정층만 남는다). "산문만 있는 수치는 쓰지 않는다"는 Phase 17 의 규칙을 우리
  코드의 상수에도 적용한 것이다.

`probe_gate.py --skip-bench` 는 이제 기존 파일의 분기 B 를 **병합 보존**한다 — 이전에는
기본 재현 경로가 §4.5 표의 절반을 덮어써 지웠다 (D46).

그림 출처: `fig1`·`fig2`·`fig4` 는 결과 JSON 만 읽고(하드코딩 제거, `fig2` 는 키가 틀려
**조용히 하드코딩으로 폴백**하던 죽은 경로였다), `fig3`(λ 궤적)·`fig5`(σ 두 구간)는 궤적
정보가 집계 JSON 에 없어 **그 자리에서 재계산**한다.

## 11. 산출물·라이선스

| 대회 필수 산출물 | 파일 |
|---|---|
| 결과보고서 | 이 문서 (`SUBMISSION.md`) |
| 시연영상 | `docs/demo_script.md` (내레이션 스크립트) + `demo.py` (촬영 대상, 결정론적) |
| 소스코드 | 저장소 전체. `README.md` / `README.en.md` / `CONTRIBUTING.md` |

라이선스 Apache-2.0. 의존성·데이터 출처·카피레프트 부재 확인: `THIRD_PARTY_LICENSES.md`
(2차 심사 라이선스 검증 대응). 라우터 추론 경로는 numpy·scipy 만 사용하며 외부 API·네트워크
호출이 없다.
